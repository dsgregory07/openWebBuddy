# Claude Code local settings — contains API keys; must never be committed
.claude/

# Runtime state
logs/

# OpenWebUI runtime data (database, secret key, uploads, vector db) — never commit
owui-data/
.webui_secret_key

# Machine-local overrides (MCPO_PORT, ...) — differs per machine, never shared
.env

# Python
net-mcp/.venv/
__pycache__/
*.pyc
